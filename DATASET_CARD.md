# DriftBench Dataset Card

## 1. Dataset Overview
**Name**: DriftBench: A Version-Pair Dataset for Browser-Extension Behavioural Drift Analysis  
**Purpose**: Benchmarking differential analysis algorithms for browser extension updates.  
**Structure**: Version pairs \((V_{t-1}, V_t)\) labeled as `Benign`, `Risky`, or `Malicious`.

---

## 2. Controlled Laboratory Extension Pair (Phase 1 Benchmarks)
- **Baseline Version (`v1_safe_note`)**:
  - Name: "Quick Note Safe"
  - Version: 1.0.0
  - Manifest Version: 3
  - Permissions: `["storage"]`
  - Host Permissions: `["https://notes.local/*"]`
  - Classification: `Benign`
  
- **Updated Version (`v2_risky_note`)**:
  - Name: "Quick Note Safe"
  - Version: 2.0.0
  - Manifest Version: 3
  - Added Permissions: `["cookies", "history"]`
  - Host Expansion: `<all_urls>`
  - Added Scripts: `background.js` (Service Worker) containing sensitive API calls, an outbound `fetch` sink, and a Base64-encoded controlled endpoint.
  - Decoded Static Endpoint Indicator: `http://analytics.untrusted-domain.com/collect`
  - Expected Label: `Risky`
  - Verified DriftWatch Rule-Engine Output (2026-08-10): `100.0/100`, `Critical`

These labels and scores apply only to controlled synthetic samples. The verified score is a current rule-engine output and may change if scoring rules change.

---

## 3. Phase 3A Dataset Methodology
DriftBench records use the schema documented in `DRIFTBENCH_SPEC.md` and implemented under `driftbench/`.

Each version-pair record must include provenance, license, label rationale, archive paths, timestamps where available, and optional SHA-256 hashes. Records may carry feature and drift vectors after analysis, but Phase 3A does not train models or report performance metrics.

Leakage-safe splits are generated by `extension_id`, not by individual row. This prevents related versions of the same extension from appearing in both train and test splits.

Controlled mutation records are synthetic laboratory artifacts. They must be marked with `controlled_mutation_type` and must not be treated as evidence of real-world prevalence.

---

## 4. Phase 3B Feature Artifacts
Phase 3B creates extracted feature artifacts from validated records. Raw DriftBench records and extracted features are separate:

- Raw records preserve provenance, labels, version identity, archive paths, and split assignments.
- Feature artifacts preserve metadata columns separately from feature columns.
- Labels and splits are metadata only and are not feature inputs.
- Feature extraction rejects invalid records before extraction.
- Analyzer failures are represented with availability flags instead of silently becoming zeros.

Generated controlled artifact location:

`artifacts/driftbench/`

Current controlled artifact summary generated from existing synthetic samples:
- `record_count`: 2
- `label_counts`: `benign_transition=1`, `risky_transition=1`
- `split_counts`: `controlled_holdout=2`
- `feature_count`: 57

These are descriptive counts for controlled local artifacts only. They are not model-performance results and should not be interpreted as real-world dataset scale.
